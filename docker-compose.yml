version: "3.9"
services:
  web:
    build:
      context: .
      dockerfile: ./docker/Dockerfile
    ports:
      - '3000:8080'
    volumes:
      - type: bind
        source: ./src
        target: /usr/src/app
    tty: true